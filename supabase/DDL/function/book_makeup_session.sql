---------------------------------------------
-- 【廃止 (2026-09-11)】本関数は削除した。
---------------------------------------------
-- 未消化チケットによる新規予約は即時確定ではなく、コーチの承認を要するように変更した
-- （Availability制約を撤廃し自由に日時選択できるようにする代わりの安全弁）。
-- 正本は function/create_session_booking_request.sql を参照。
---------------------------------------------
DROP FUNCTION IF EXISTS public.book_makeup_session(uuid, date, time);
