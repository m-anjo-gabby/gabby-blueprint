/**
 * 生徒入室（実質的なレッスン開始）を起点とした、残り時間の警告表示と自動終了までの猶予（ミリ秒）。
 * コーチ・生徒双方のライブセッションルームで共通して使用する。
 * PCOのため、制限時間25分、自動切断30分（猶予5分）を制限時間5分、自動切断分(猶予1分)にする
 * 制限時間はクライアント依存の為、実際にはcom_t_session連動のサーバー時刻で制御する必要がある
 */
export const LIVE_SESSION_WARNING_AFTER_MS = 5 * 60 * 1000;
export const LIVE_SESSION_END_AFTER_MS = 6 * 60 * 1000;
