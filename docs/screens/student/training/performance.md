# トレーニング記録（studentアプリ）

## 概要

- アプリ: `student`
- パス: `/training/performance`
- 対象ロール: 生徒
- 目的: 単語帳ドリルとスプリントを合わせた月間のトレーニング実績（トレーニング日数・総発話回数・
  単語/フレーズ数・スプリント本数/回答数）を確認し、日別の実施内容や各履歴画面への入口とする。

## この画面に来る経路

- ダッシュボードの「今週のトレーニング」の「記録を見る」リンク、または「メニュー」の「トレーニング記録を見る」から
  遷移する。単語帳履歴・スプリント履歴の各画面からも、戻るボタンで遷移してくる。
- アプリシェル内の画面で、共通ナビゲーションは「トレーニング」タブがアクティブになる。

## 画面の構成

1. **ヘッダー** — 戻るボタン、画面タイトル「トレーニング記録」、説明文、月切替（前月・翌月・今月に戻る）。
   月切替はスクロールしても画面上部に固定表示される
2. **○月のまとめ** — 当月のトレーニング日数、発話回数（単語帳・スプリント合算）
3. **トレーニング別** — 単語帳（単語数・フレーズ数）、スプリント（実施回数・回答数）のカード。
   それぞれの履歴画面への入口を兼ねる
4. **実施カレンダー** — 曜日の列を揃えた当月のカレンダー。実施した日を塗りつぶし、
   タップするとその日の内訳をポップオーバー表示する

PC（lg以上）では 2・3 を左、4 を右に並べる。モバイルでは上から順に縦に並べる。

## 表示要素・操作

| 要素 | 表示条件・内容 | 操作した時の挙動 |
|---|---|---|
| 戻るボタン | 常時表示 | 直前の画面へ戻る（履歴が無い場合はダッシュボードへ遷移する） |
| 月切替の矢印（前月・翌月） | 常時表示 | 対象月を切り替えて集計を再取得する |
| 「今月に戻る」ボタン | 当月以外の月を表示している場合のみ | 当月の表示に戻す |
| トレーニング日数 | 常時表示 | 当月中、単語ドリルまたはスプリントを1回以上実施した日数を表示 |
| 発話回数 | 常時表示 | 単語ドリル・スプリントいずれかで発話評価を行った回数の合計を表示 |
| 「単語帳」カード | 常時表示 | タップで[単語帳の履歴画面](./word-history.md)（該当月指定）へ遷移する |
| 「スプリント」カード | 常時表示。「※ 回答数にドリルモードの回答は含みません」という注記を表示する | タップで[スプリントの履歴画面](./sprint-history.md)（該当月指定）へ遷移する |
| カレンダーの日付 | その日にトレーニング実績がある場合、ブランド色で塗りつぶして表示 | タップでポップオーバーを開き、その日の単語帳（単語数/フレーズ数）、スプリント（回数/回答数）、発話回数の内訳を表示する |

## 状態

特別なローディング・エラー状態は無く、月の切り替え中は月切替の月表示がスピナーに
置き換わる程度で、それ以外は常に確定したデータを表示する（実績が無い日・月はカードの数値が
0またはグレー表示になる）。

## 関連する業務フロー仕様書

この画面は実績の閲覧・各履歴画面への導線であり、複数画面をまたぐ業務フローは特に無い。

## 実装参照（エンジニア向け）

- `apps/student/app/(app)/(shell)/training/performance/page.tsx`
- `apps/student/app/(app)/(shell)/training/performance/_components/TrainingPerformance.tsx`
- `apps/student/app/(app)/(shell)/training/_components/`（`MonthSwitcher` / `StatTile` / `HistoryParts`。履歴画面と共通）
- `apps/student/actions/performanceAction.ts`（`getUserTrainingPerformanceAction`）
- `packages/lib/hooks/useMonthNavigator.ts`（単語履歴・スプリント履歴画面と共通の月ナビゲーション）
- 指標・教材種別のアイコンと分類色: `packages/lib/content/ui.ts`（`getTrainingMetricConfig` / `getContentTypeConfig`）
