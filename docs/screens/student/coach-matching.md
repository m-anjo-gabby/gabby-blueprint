# 専属コーチを探す（studentアプリ）

## 概要

- アプリ: `student`
- パス: `/coach-matching`
- 対象ロール: 生徒（ライブセッション付きプランの契約者のみ利用可能）
- 目的: 週あたりのセッション枠（コマ）ごとに、担当してほしいコーチへマッチングをリクエストする。
  コーチが承認すると、契約期間分のセッションが自動で予約される。

## この画面に来る経路

- [ライブセッション・ホーム](./live-room/hub.md)（`/live-room`）の「専属コーチを探す」ボタンから遷移する
  （アプリシェルでは「ライブセッション」タブがアクティブになる）。

## 画面の構成

1. **ヘッダー** — 画面タイトル、ライブセッション・ホームへ戻るボタン、絞り込み後のコーチ件数バッジ、
   セッション枠数の案内文
2. **セッション枠の状況** — 契約上の週あたりコマ数ぶんの状況カード（マッチング済み／承認待ち／
   未マッチング）
3. **コーチを選ぶ** — 検索・絞り込みフィルターと、コーチ一覧（カード形式）
4. **リクエストダイアログ**（コーチカードから開く） — 空き時間カレンダーから希望日時を選び、
   リクエストを送信する

## 表示要素・操作

| 要素 | 表示条件・内容 | 操作した時の挙動 |
|---|---|---|
| セッション枠カード | 契約の週あたりコマ数ぶん表示。各枠の状態バッジ（「マッチング済み」緑／「承認待ち」黄／「未マッチング」グレー）、マッチング済み・承認待ちの場合は担当コーチ名・曜日・時間帯を表示 | 表示のみ（キャンセルボタンは別項参照） |
| 前回否認理由 | 未マッチング状態の枠で、直前に否認された履歴がある場合 | 「前回否認理由: 〜」を赤系の注記で表示 |
| 「リクエストを取消す」ボタン | 承認待ち状態の枠に表示 | 確認ダイアログの上でリクエストを取消し、枠は未マッチング状態に戻る |
| コーチ名検索欄 | 常時表示（未マッチング枠が1つ以上ある場合） | 入力した文字列を含む名前のコーチのみに絞り込む |
| 曜日フィルター（複数選択可） | 同上 | 選択した曜日に対応可能時間を持つコーチのみに絞り込む |
| 時間帯フィルター（複数選択可） | 同上 | 選択した大まかな時間帯に該当する対応可能時間を持つコーチのみに絞り込む |
| 「リセット」ボタン | 何らかのフィルターが設定されている場合のみ表示 | 検索条件・曜日・時間帯の選択をすべて解除する |
| コーチカード | 未マッチングの枠が1つ以上ある場合に一覧表示。アイコン、名前、国旗、指導歴、自己紹介文（省略表示）、対応可能時間帯のチップ（フィルター条件に一致するものが優先的に先頭表示） | — |
| 「プロフィール」ボタン | 各コーチカードに表示 | コーチの詳細プロフィール（学歴・資格・指導歴・職務経験・自己紹介・紹介動画）をダイアログで表示 |
| 「カレンダーからリクエストする」ボタン | コーチに対応可能時間が1件以上ある場合のみ活性 | リクエストダイアログを開く |
| コーチ一覧が空の場合 | リクエスト可能なコーチが1人もいない場合 | 「現在リクエスト可能なコーチがいません。」 |
| フィルター結果が0件の場合 | 検索・絞り込み条件に一致するコーチがいない場合 | 「条件に合うコーチが見つかりませんでした／曜日・時間帯の条件を変更してお試しください」 |
| 全枠マッチング完了時の表示 | 未マッチングの枠が無い場合 | 「すべての枠のマッチングが完了しています」と表示し、検索フィルター・コーチ一覧は表示されない |

## リクエストダイアログ

- コーチの対応可能時間を、25分セッションの開始候補として週間カレンダー形式（曜日×30分刻み）で
  表示する。時刻は生徒自身のタイムゾーンに変換して表示される。
- 〇印のセルが申請可能、×印のセルは既に確定済み・承認待ちの予定と重複するため選択不可
  （表示上の簡易チェックであり、最終的な整合性はサーバー側で判定される）。
- セルを選択すると、「毎週◯曜日 開始時刻〜終了時刻」の確定内容と、初回のライブセッション予定日
  （現在時刻から実時間で24時間以上先となる、選択した曜日・時刻の直近の回）が表示される。
- リクエスト対象の枠（コマ）が複数ある場合はプルダウンで選択、1つしかない場合は自動選択される。
- 送信すると当該枠は「承認待ち」状態になり、コーチの承認を待つ（即時確定ではない）。

## 状態

| 状態 | 表示内容 | 発生条件 |
|---|---|---|
| ライブセッション付きプラン未契約 | 「この機能はライブセッション付きプランの方のみご利用いただけます／ご不明な点はサポートまでお問い合わせください」 | ライブセッション付き契約の有効なチケットを保持していない場合。この場合、画面の他の要素は一切表示されない |
| リクエスト取消処理中 | 対象枠の「リクエストを取消す」ボタンがスピナー表示になる | 取消処理の送信中 |
| リクエスト送信中 | ダイアログの送信ボタンがスピナー表示になり非活性 | リクエスト送信処理中 |

## 実装参照（エンジニア向け）

- `apps/student/app/(app)/(shell)/coach-matching/page.tsx`
- `apps/student/app/(app)/(shell)/coach-matching/_components/CoachMatchingView.tsx`
- `apps/student/app/(app)/(shell)/coach-matching/_components/CoachSearchFilters.tsx`
- `apps/student/app/(app)/(shell)/coach-matching/_components/CoachCard.tsx`
- `apps/student/app/(app)/(shell)/coach-matching/_components/CoachAvailabilityCalendar.tsx`
- `apps/student/app/(app)/(shell)/coach-matching/_components/RequestDialog.tsx`
- コーチプロフィール表示: `packages/lib/components/common/CoachProfileDialog.tsx`
- 関連アクション/RPC: `getMyLiveSessionTickets`, `getMySlotStatus`, `getCoachBrowseList`,
  `createMatchingRequest`, `cancelMatchingRequest`（`apps/student/actions/matchingAction.ts`）
- 備考: `testing/e2e/specs/_INDEX.md`によれば、マッチング業務フロー（`matching/`）の仕様書は
  本調査時点では未作成（`booking/individual-booking-and-reschedule.md`はセッション予約・振替に
  関する別フローであり、この画面の初回マッチングとは対象が異なるためリンクしていない）。
