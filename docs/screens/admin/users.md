# ユーザー管理（adminアプリ）

## 概要

- アプリ: `admin`
- パス: `/users`
- 対象ロール: 管理者
- 目的: 各テナント（顧客）に所属する生徒・コーチ・管理者アカウント（`com_m_user`）の作成・編集、
  権限（ロール）設定、ライセンス割当、生徒のトレーニング進捗（ステージ・レベル）の補正、
  および障害対応用の代理ログインを行う。

## この画面に来る経路

- サイドバーの「ユーザー管理」から遷移する。ダッシュボードの「ユーザー管理」カードからも遷移できる。

## 画面の構成

1. **ヘッダー** — 画面タイトル、説明文、「一括登録」ボタン、「新規登録」ボタン
2. **ユーザー一覧テーブル** — 検索欄、区分フィルタ（管理者・生徒・コーチ）、顧客フィルタ、
   ページネーション、一覧表（1ページ10件）
3. **ユーザー登録・編集ダイアログ**
4. **ライセンス管理ダイアログ**（生徒のみ、一覧の「ライセンス」欄から開く）
5. **ステージ・レベル管理ダイアログ**（生徒のみ、一覧の「ステージ」ボタンから開く）
6. **代理ログインダイアログ**（本登録済みの生徒・コーチのみ、一覧の「代理ログイン」ボタンから開く）
7. **CSV一括登録ダイアログ**（生徒のみ対象）

## 表示要素・操作

| 要素 | 表示条件・内容 | 操作した時の挙動 |
|---|---|---|
| 検索欄 | 常時表示 | キーワード検索。Enterまたは「検索」ボタンで実行し、1ページ目に戻る |
| 区分フィルタ | 常時表示 | 「すべての区分」「生徒」「コーチ」「管理者」を切り替えて絞り込む |
| 顧客フィルタ | 常時表示 | 検索式セレクトで顧客を選択して絞り込む（未選択時は「すべての顧客」で全件対象）。選択すると1ページ目に戻る |
| 一覧の「ユーザー」列 | 氏名・メールアドレス | 表示のみ |
| 一覧のステータスバッジ | ログイン実績あり:「アクティブ」／招待確認済み・未ログイン:「招待確認済」／招待メール未送信・送信失敗:「送信失敗」（ツールチップにエラー内容）／それ以外:「招待中」 | 表示のみ |
| 一覧の「所属顧客」列 | 顧客名。未所属なら「未所属」 | 表示のみ |
| 一覧の「区分」列 | 生徒／コーチ／管理者 | 表示のみ |
| 一覧の「ライセンス」列 | 生徒のみ表示（生徒以外は「対象外」）。プラン名・期限、状態に応じ「未設定」「[開始待ち]」「[期限切れ]」のラベル | 本登録済みの生徒のみクリック可能。クリックでライセンス管理ダイアログを開く |
| 一覧の「代理ログイン」ボタン | 本登録済み（招待中でない）の生徒・コーチのみ表示（管理者は対象外） | 代理ログインダイアログを開く |
| 一覧の「ステージ」ボタン | 生徒のみ表示 | ステージ・レベル管理ダイアログを開く |
| 一覧の「編集」ボタン | 全ユーザーに表示 | ユーザー編集ダイアログを開く |
| 「一括登録」ボタン | 常時表示 | CSV一括登録ダイアログを開く（生徒のみ対象） |
| 「新規登録」ボタン | 常時表示 | ユーザー登録ダイアログを開く |

## ユーザー登録・編集ダイアログ

- 新規登録時は作成方法を選択できる: 「招待メールを送信」（本登録リンクをメール送付、相手が
  パスワード設定するまで招待中の状態）または「即時作成」（確認メール無しでその場で有効化。
  管理者がその場でパスワードを設定する）
- 入力項目: メールアドレス、氏名、所属顧客（検索式）、ユーザー種別（生徒／管理者／コーチ）、
  ロール（ユーザー種別に応じて選べるロールが変わる。共通ロールは種別を問わず選択可）、
  新規登録・生徒の場合は初期ライセンス（有効な契約から選択。生徒には必須）、即時作成の場合は
  パスワード・パスワード確認
- ユーザー種別を変更すると、その種別で選択不可になったロールは自動的に選択解除される
- 招待中（未登録）のユーザーを編集する場合、氏名・メールアドレス・所属顧客・種別・ロールは変更
  できず、代わりに「招待メールを再送する」ボタンのみ表示される
- 編集モードではメールアドレス・初期ライセンス欄は表示・変更されない（メールアドレスは編集不可、
  ライセンスは別画面のライセンス管理ダイアログで扱う）
- 「確認画面へ進む」でバリデーション後に確認表示へ切り替わり、「確定して保存」（新規・即時作成時は
  「アカウントを作成する」）で送信する

## ライセンス管理ダイアログ（生徒のみ）

- 「一覧・履歴」タブ: その生徒に紐づく全ライセンスの履歴を表示（無効化済み・解除済み・期間終了は
  グレー表示）。有効なライセンスには編集ボタンと無効化ボタンがある
- 「新規割当」／「編集」タブ: 契約プラン（新規時は残枠のある有効契約から選択、編集時は変更不可の
  ため現在の割当プラン名を表示のみ）、ライセンス開始日・終了日、備考（変更理由等）
  - 新規割当時: 契約期間の範囲内であることをサーバー側で常に検証する
  - 編集時: 契約期間内チェックは行わない（個別の生徒対応として、契約の終了日を超える延長を
    含め期限を自由に変更できる仕様。画面上にもその旨の案内文を表示する）
  - 新規・編集とも、同じ生徒の他のライセンスと期間が重複する場合は割当・変更できない
    （契約更新に伴う次タームライセンスの前倒し登録など、期間が重ならない複数ライセンスの
    保有は許容される。DB側にも排他制約があり、同時操作による重複登録は最終的に防止される）
- 無効化すると即座にシステム利用が不可になり、未実施の今後のセッションもキャンセルされるが、
  実施済みのセッション履歴・チャット・宿題は削除されずチケット消化数も戻らない

## ステージ・レベル管理ダイアログ（生徒のみ）

- 「問題種別ごと」タブ: トレーニングの問題種別（複数）ごとに現在のレベルを表示し、選択式で
  個別に変更・保存できる（コーチの誤操作補正等を想定）
- 「ステージ」タブ: 現在のステージを表示し、目標ステージを選択する
  - 現在より上のステージへ引き上げる場合、到達条件を満たしていない問題種別があれば警告を表示し、
    実行すると該当レベルが必要最小値まで強制的に引き上げられる（条件を満たす種別は変更されない）
  - 現在より下のステージへ引き下げる場合、対象ステージちょうどの基準値まで該当レベルがリセット
    される旨の警告を表示する

## 代理ログインダイアログ

- 実行理由（チケット番号・問合せ内容等）の入力が必須
- 実行すると対象ユーザーとして生徒ポータルまたはコーチポータルへの代理ログイン用タブが新規に開く。
  画面上の操作は全て本人の操作として実際に反映され、実行内容は監査ログに記録される旨の注記あり

## CSV一括登録ダイアログ（生徒のみ対象）

- 手順: 1. 所属顧客を選択 → 2. 初期ライセンス（有効な契約）を選択 → 3. CSVファイルをドラッグ&ドロップ
  またはクリックして選択（1回最大30件、サンプルCSVのダウンンロードリンクあり）
- CSVは「メールアドレス」「名前」列（英語ヘッダーの`email`/`user_name`も可）を読み取り、
  メール形式・名前未入力をその場でチェックし、内容確認テーブルに一覧表示する
- 顧客に有効な契約が無い場合、ライセンス選択欄に警告が表示され、生徒を含む一括登録はライセンス
  割当が必須のため進められない
- 「一括登録を開始」を押すとユーザー作成とライセンス割当を行い、各行ごとに「データ作成済/作成失敗」
  「メール送信済/メール送信失敗」の結果がレポート表示される
- 作成されるユーザーは常に生徒種別(user_type=1)固定であり、コーチ・管理者の一括登録はできない

## 状態

| 状態 | 表示内容 | 発生条件 |
|---|---|---|
| データ読み込み中 | スピナー付きプレースホルダー | ユーザー一覧取得中（Suspense） |
| 検索結果が空 | 「データが見つかりませんでした。」 | 検索・フィルタ条件に一致するユーザーが0件の場合 |
| 割当可能なライセンスなし | 選択欄に「割当可能な契約プランがありません」、登録・保存ボタンが非活性 | 対象顧客に有効な契約が無い場合 |

## 実装参照（エンジニア向け）

- `apps/admin/app/(app)/users/page.tsx`
- `apps/admin/app/(app)/users/_components/user-data-table.tsx`
- `apps/admin/app/(app)/users/_components/columns.tsx`
- `apps/admin/app/(app)/users/_components/UserTypeFilter.tsx`
- `apps/admin/components/common/ClientFilter.tsx`（顧客フィルタ。`contracts`一覧とも共用する共通
  コンポーネント。URLクエリパラメータ`clientId`と同期）
- `apps/admin/app/(app)/users/_components/UserFormDialog.tsx`
- `apps/admin/app/(app)/users/_components/LicenseFormDialog.tsx`
- `apps/admin/app/(app)/users/_components/SprintProgressFormDialog.tsx`
- `apps/admin/app/(app)/users/_components/ImpersonateButton.tsx`
- `apps/admin/app/(app)/users/_components/UserBulkImportDialog.tsx`
- `apps/admin/actions/adminUserAction.ts`（`getUsers`, `createUser`, `createUserDirect`,
  `updateUser`, `resendInvite`, `getRoles`, `bulkCreateUsers`）
- `apps/admin/actions/adminContractAction.ts`（`getActiveContractsByClient`,
  `getLicenseTimeline`, `assignLicenseToUser`, `updateUserLicense`, `invalidateUserLicense`,
  `bulkAssignLicenses`）
- `apps/admin/actions/adminStudentProgressAction.ts`（`getStudentSprintProgress`,
  `updateStudentSprintLevel`, `setStudentSprintStage`）
- `apps/admin/actions/adminImpersonationAction.ts`（`startImpersonation`。監査ログ:
  `com_t_admin_impersonation_log`）
- 対象テーブル: `com_m_user`
- `apps/admin/actions/adminClientAction.ts`（`getClientsFilter`。顧客フィルタの選択肢取得）
