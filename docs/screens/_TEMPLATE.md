<!--
  新規画面仕様書のひな形。コピーして使う（このコメントブロックは削除する）。
  記述方針（詳細は ../README.md 参照）:
  - 実装に詳しくない読者（テスト専任メンバー等）が読める言葉で書く。コンポーネント名に
    頼った説明にしない（「実装参照」節にファイル名としてまとめるのは可）
  - 「今の画面」だけを書く。変更履歴・経緯は書かない
  - 複数画面をまたぐ業務フローは書かず、該当する testing/e2e/specs/ にリンクする
  - 保存したら _INDEX.md にも1行追加する
-->

# <画面名>（<app>アプリ）

## 概要

- アプリ: `<student|coach|admin>`
- パス: `/<route>`
- 対象ロール: <この画面を見られるロール>
- 目的: <この画面で何ができるか、1〜2文>

## この画面に来る経路

<どの画面からどう遷移してくるか>

## 画面の構成

<セクション単位で、プレーンな言葉で説明する（実装のコンポーネント分割そのままではなく、
 ユーザーから見た意味のある単位で区切る）>

## 表示要素・操作

<テストケースにそのまま転用できる形（表）で列挙する>

| 要素 | 表示条件・内容 | 操作した時の挙動 |
|---|---|---|
| | | |

## 状態

<この画面全体、または主要な要素が取り得る状態を列挙する（読み込み中・空・エラー等）>

| 状態 | 表示内容 | 発生条件 |
|---|---|---|
| | | |

## 関連する業務フロー仕様書

- [ ](../../../testing/e2e/specs/...) — <この画面が関わる業務フローの概要>

## 実装参照（エンジニア向け）

- `<主要なファイルパス>`
- 関連RPC: `<あれば>`
