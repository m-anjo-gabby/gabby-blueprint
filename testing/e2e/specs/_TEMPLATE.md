<!--
  新規機能仕様書のひな形。コピーして使う（このコメントブロックは削除する）。
  記述方針（詳細は ../README.md 参照）:
  - 「今の仕様」だけを書く。変更履歴・経緯は書かない
  - status値等の共通用語は書かず _GLOSSARY.md にリンクする
  - 肥大化したらサブフロー単位でファイル分割する
  - 保存したら _INDEX.md にも1行追加する
-->

# <機能名>

## 概要

<この機能が何を実現するか。1〜3文。ビジネス背景の説明は不要（「なぜ」ではなく「何を」）>

## 関与ロール・画面

<画面仕様書(docs/screens/)が既にあればリンクする。無ければコンポーネント/アクション名のみ>

| ロール | 画面 | パス | 主なコンポーネント/アクション |
|---|---|---|---|
| <生徒/コーチ/アドミン> | <画面名> | `<app>` `/<route>` | `<ファイル名>`。画面仕様: [docs/screens/...](../../../../docs/screens/...) |

## 前提条件

<この機能を実行するために必要な状態。契約・ライセンス・既存データ等。使用するアカウントは
 まず ../../FIXTURES.md の固定アカウントで足りないか確認し、無ければ都度シードする方針を書く>

## フロー（正常系）

<番号付きステップ。誰が・何を・結果として何が起きるか（画面遷移・通知含む）を簡潔に。
 分岐がある場合は 3a / 3b のように枝分かれさせる>

1. ...
2. ...

## 異常系・バリデーション一覧

<テストケースにそのまま転用できる形（表）で列挙する。「発生層」はUI/RPC/両方の3種類
 （凡例は ../README.md 参照）。UI側に導線が無く直接RPCを叩かないと再現できない場合は
 「RPC（UI導線なし）」のように注記する>

| # | 条件 | 期待結果 | 発生層 |
|---|---|---|---|
| 1 | | | |

## 関連RPC・テーブル

- RPC: `xxx`
- テーブル: `xxx`
- 実装参照: `<主要な packages/lib, apps/*/actions のファイルパス>`
- 用語（status値等）: [_GLOSSARY.md](../_GLOSSARY.md)

## E2Eテストケース候補

<Playwrightでのテストシナリオ起票の下書き。優先度は 高(必須)/中/低 の3段階>

| 優先度 | シナリオ | 概要 |
|---|---|---|
| 高 | | |
