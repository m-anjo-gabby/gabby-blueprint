import { NoticeType } from '@gabby/types/notice';

/** English notice-type labels for the Coach portal (structural color/badgeClass still comes from the shared NOTICE_TYPES). */
export const NOTICE_TYPE_LABEL_EN: Record<NoticeType, string> = {
  INFO: 'Notice',
  CAMPAIGN: 'Campaign',
  MAINTENANCE: 'Maintenance',
  UPDATE: 'Update',
};

export const NOTICE_IMPORTANT_LABEL_EN = 'Important';
